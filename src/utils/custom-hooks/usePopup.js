// import { useState } from "react";

// const usePopup = () => {
//   //instantiate new isShowing and setIsShowing state values to the current view
//   const [isShowing, setIsShowing] = useState(false);
//   //function toggle that changes the value to be the opposite of what it is currently
//   function toggle() {
//     setIsShowing(!isShowing);
//   }
//   // returning value of isShowing and toggle the function from the hook
//   //so the component has access to them.
//   return {
//     isShowing,
//     toggle,
//   };
// };
